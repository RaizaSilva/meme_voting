<% @memes.each do |meme| %>
  <td>
    &nbsp
    <div id="meme_listing">
      <%= link_to (image_tag attachment_url(meme, :meme_img, :fill, 100, 100)), photo_vote_url(@photo,meme), method: :patch  %>
    </div>
  </td>
<% end %>
